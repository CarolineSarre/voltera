/**
 * Demonstração de teste para ApiResult.svelte
 *
 * // import { render } from '@testing-library/svelte';
 * // import ApiResult from '../ApiResult.svelte';
 *
 * // describe('ApiResult', () => {
 * //   it('exibe resultado de sucesso', () => {
 * //     const { getByText } = render(ApiResult, {
 * //       props: { name: 'João', age: 28, count: 123, error: null }
 * //     });
 * //
 * //     expect(getByText(/28 anos/i)).toBeInTheDocument();
 * //     expect(getByText(/123 registros/i)).toBeInTheDocument();
 * //   });
 * //
 * //   it('exibe mensagem de erro', () => {
 * //     const { getByText } = render(ApiResult, {
 * //       props: { name: 'João', age: null, count: null, error: 'Falhou' }
 * //     });
 * //
 * //     expect(getByText(/falhou/i)).toBeInTheDocument();
 * //   });
 * // });
 */