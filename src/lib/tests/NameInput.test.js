/**
 * Demonstração de teste para NameInput.svelte
 *
 * // import { render, fireEvent } from '@testing-library/svelte';
 * // import NameInput from '../NameInput.svelte';
 *
 * // describe('NameInput', () => {
 * //   it('mostra valor inicial e dispara submit', async () => {
 * //     const { component, getByRole } = render(NameInput, { props: { value: 'Ana' } });
 * //     const mock = vi.fn();
 * //     component.$on('submit', mock);
 * //
 * //     await fireEvent.input(getByRole('textbox'), { target: { value: 'Carlos' } });
 * //     await fireEvent.submit(getByRole('form'));
 * //
 * //     expect(mock).toHaveBeenCalled();
 * //   });
 * // });
 */
