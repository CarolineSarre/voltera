/**
 * Demonstração de teste para PageHeader.svelte
 *
 * // import { render } from '@testing-library/svelte';
 * // import PageHeader from '../PageHeader.svelte';
 *
 * // describe('PageHeader', () => {
 * //   it('exibe título e subtítulo', () => {
 * //     const { getByRole, getByText } = render(PageHeader, {
 * //       props: { title: 'Bem‑vindo', subtitle: 'Sub' }
 * //     });
 * //
 * //     expect(getByRole('heading', { level: 1 })).toHaveTextContent('Bem‑vindo');
 * //     expect(getByText('Sub')).toBeInTheDocument();
 * //   });
 * // });
 */