/**
 * Demonstração de teste para Box.svelte
 *
 * // import { render } from '@testing-library/svelte';
 * // import Box from '../Box.svelte';
 *
 * // describe('Box', () => {
 * //   it('deve renderizar o slot interno', () => {
 * //     const { getByText } = render(Box, { slots: { default: '<p>Oi!</p>' } });
 * //     expect(getByText('Oi!')).toBeInTheDocument();
 * //   });
 * // });
 */
