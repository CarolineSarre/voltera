/**
 * Demonstração de teste para WhatsappButton.svelte
 *
 * // import { render } from '@testing-library/svelte';
 * // import WhatsappButton from '../WhatsappButton.svelte';
 *
 * // describe('WhatsappButton', () => {
 * //   it('possui link para WhatsApp', () => {
 * //     const { getByRole } = render(WhatsappButton);
 * //     const link = getByRole('link');
 * //     expect(link.getAttribute('href')).toMatch(/^https:\/\/wa\.me\/|^whatsapp:/);
 * //   });
 * // });
 */